import React from 'react';
import styled from 'styled-components';

const StyledButton = styled.button`
  border: none;
  background: none;
`;

const Button = () => <StyledButton>Generic button</StyledButton>;

export default Button;
